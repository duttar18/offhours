import React from 'react'
import styled from 'styled-components';
import { Typography } from '@material-ui/core';

const About = () => { 
    return ( 
        <Typography variant="h1" color="primary">What is Offhours</Typography>
    )
}
 export default About; 